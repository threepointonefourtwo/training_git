#!/usr/bin/env  python3

import xml.etree.ElementTree as ET

tree = ET.parse('note.xml')

root = tree.getroot()

for element in root.iter():
	print("----{0}({1})".format(element.tag,() for i in list(element.attrib.keys()))))
